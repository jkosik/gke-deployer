# Templated from parsed dynamic inventory (inventory-dynamic-gcp.yaml) or gcloud output,
# saved as inventory-ephemeral.yaml and used by ansible-playbook
---
all:
  hosts:
    jh:
      ansible_host: CHANGEME